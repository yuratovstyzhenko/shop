import { ColorBlockDirective } from './color-block.directive';

describe('ColorBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBlockDirective();
    expect(directive).toBeTruthy();
  });
});
